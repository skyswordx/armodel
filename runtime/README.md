# Runtime

This directory exposes a stable inference-facing package for the Roboclaw arm stack.

The runtime should read manifests and config files.
It should not depend on post-training directory internals or hard-coded local model paths.
