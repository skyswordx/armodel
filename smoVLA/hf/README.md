# Hugging Face Sources

Use this area to record exactly which Hugging Face model revision was used.

- `manifests/` contains model source manifests.
- `snapshots/` is a local download target and is ignored by Git.

Prefer immutable revisions such as a commit SHA for reproducible training and export.
