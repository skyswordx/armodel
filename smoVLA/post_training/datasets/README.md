# Dataset Manifests

Keep dataset manifests and small schema examples here.
Do not commit raw or processed training datasets.

The runtime and training scripts should identify datasets through manifest files instead of hard-coded local paths.
