# Vision NPU

This directory owns vision preprocessing, calibration, and NPU backend adapters.

Keep model-family training code in `smoVLA/`.
Keep runtime-facing model loading in `runtime/`.
Use this directory for backend-specific conversion notes, calibration sample manifests, and preprocessing code shared by export and inference.
